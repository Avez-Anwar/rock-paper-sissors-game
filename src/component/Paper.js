import React from "react";
import SportsSoccerIcon from "@mui/icons-material/SportsSoccer";
import NoteIcon from "@mui/icons-material/Note";

function Paper() {
  return (
    <div>
      <NoteIcon sx={{ fontSize: 100, color: "grey", padding: "0 50px" }} />
    </div>
  );
}

export default Paper;
